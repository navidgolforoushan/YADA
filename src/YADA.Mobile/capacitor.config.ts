import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ngolforoushan.jumpstarter',
  appName: 'jumpstart',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
